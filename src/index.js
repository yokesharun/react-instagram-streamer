import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import InstagramStreamer from './InstagramStreamer';

ReactDOM.render(
  <React.StrictMode>
    <InstagramStreamer />
  </React.StrictMode>,
  document.getElementById('root')
);
